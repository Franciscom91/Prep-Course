1) Prototype: Cada clase tiene una forma rápida y eficaz de crear un método (ahorrando memoria y tiempo). A traves de herencias podemos utilizar la propiedad .prototype.

2) Constructor: El constructor es un único método que se crea al inicializar la clase al cual se le puede pasar parámetros